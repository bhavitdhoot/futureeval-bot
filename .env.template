# For running the bot on your own computer only. On GitHub, use repository
# secrets instead (see SETUP.md). Copy this file to .env and fill it in.
# Never commit .env.

METACULUS_TOKEN=REPLACE_ME
OPENROUTER_API_KEY=REPLACE_ME

# Optional: AskNews (either the ID + secret pair, or a single API key)
# ASKNEWS_CLIENT_ID=REPLACE_ME
# ASKNEWS_SECRET=REPLACE_ME
# ASKNEWS_API_KEY=REPLACE_ME
